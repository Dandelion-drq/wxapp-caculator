/* 清除button组件默认样式 */
button {
  border: none;
  background-color: transparent;
  border-radius: 0;
}
button::after {
  border: none;
}

.flex {
  display: flex;
}

.flex-dr-col {
  flex-direction: column;
}